You are running inside a podman container (base image: Fedora 44).
You can install packages as required from Fedora repositories.
You only have access to the current working directory and PI Agent's directory.